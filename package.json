{
    "name": "rightmove-scraper",
    "version": "1.0.0",
    "description": "Apify actor for scraping Rightmove property listings",
    "main": "src/main.js",
    "scripts": {
        "start": "node src/main.js",
        "test": "jest",
        "lint": "eslint src/**/*.js",
        "build": "docker build -t rightmove-scraper .",
        "deploy": "apify push"
    },
    "dependencies": {
        "apify": "^3.1.1",
        "puppeteer": "^19.0.0"
    },
    "devDependencies": {
        "jest": "^29.0.0",
        "eslint": "^8.0.0",
        "@apify/eslint-config": "^0.3.0"
    }
}